import React from 'react';
import DirectoryHeader from './DirectoryHeader';

export default () => (
  <div className="wrapper">
    <DirectoryHeader />
    <p>This would be the upload UI.</p>
  </div>
)